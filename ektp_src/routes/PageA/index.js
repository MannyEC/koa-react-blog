import PageAContainer, { initLoader } from './containers/PageAContainer';
import PageA from './modules';

export const pageAReducer = {
  PageA,
};

export const PageAinitLoader = initLoader;
export default PageAContainer;
